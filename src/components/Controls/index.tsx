const Controls = () => (
  <div className="controls">
    <div className="controls-message-box">
      <input type="text" placeholder="Type a message" />
      <button>{'>'}</button>
    </div>
  </div>
);

export default Controls;
